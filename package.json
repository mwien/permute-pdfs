{
  "name": "permute_pdfs",
  "version": "1.0.0",
  "description": "Merge PDFs in randomly sampled permutations.",
  "main": "app.js",
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marcel Wienöbst",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.24.0"
  },
  "dependencies": {
    "@zip.js/zip.js": "^2.7.53",
    "lodash": "^4.17.21",
    "pdf-lib": "^1.17.1"
  }
}
